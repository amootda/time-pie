# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# App URL (OAuth 리다이렉트용)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Service Role Key (서버 사이드에서만 사용)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Kakao OAuth 설정 방법:
# 1. Supabase Dashboard > Authentication > Providers > Kakao 활성화
# 2. Kakao Developers (https://developers.kakao.com) 에서 앱 생성
# 3. 플랫폼 > Web > 사이트 도메인 추가 (예: http://localhost:3000)
# 4. Kakao 로그인 > Redirect URI 설정:
#    - https://your-project.supabase.co/auth/v1/callback
# 5. REST API 키를 Supabase Kakao Provider Client ID에 입력
# 6. Client Secret을 Supabase Kakao Provider Client Secret에 입력
